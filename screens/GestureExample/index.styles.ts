import { ViewStyle } from "react-native";

const card: ViewStyle = {
  marginVertical: 16,
};

const container: ViewStyle = {
  alignItems: "center",
};

export const styles = {
  card,
  container,
};
